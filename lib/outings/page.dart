import 'package:flutter/material.dart';

 class OutingsPage extends StatelessWidget {
   const OutingsPage({Key key}) : super(key: key);
 
   @override
   Widget build(BuildContext context) {
     return Container(
       child: Center(child:Text("logeado")),
     );
   }
 }